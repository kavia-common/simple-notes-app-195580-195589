# Notes Backend Environment Variables (example)
#
# Copy to your environment management system; do not commit real secrets.

# Comma-separated list of exact origins allowed by CORS.
# Example: http://localhost:3000,http://127.0.0.1:3000
ALLOWED_ORIGINS=

# Optional regex allowing additional origins.
# Default (if unset) allows Kavia preview URLs like:
#   https://vscode-internal-<something>:3000
ALLOWED_ORIGIN_REGEX=
